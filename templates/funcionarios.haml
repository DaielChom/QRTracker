/ Extends base
{% extends 'base.html' %}

/ Block
{% block content %}
{% from '_macro.html' import render_field %}

%form.raleway.submit
  / Input necessary for CSRFProtect
  %input{type:'Hidden', name:"csrf_token", value:' {{ csrf_token() }} '}

  / Rende Inputs in a loop
  {% for input in funcionario_form %}

  {{ render_field( input.label, class="" )}}
  {{ render_field( input, class="", id="", pattern="{ 4, 25 }", required="True", pattern ="[a-zA-Z0-9 ]{4,25}") }}

  {% endfor %}

  %input#enviar{type:"submit", value:"AGREGAR"}
{% endblock %}
